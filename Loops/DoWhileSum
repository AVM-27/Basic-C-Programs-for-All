/*
 * Filename    : loops/dowhilesum.c
 * Description : Calculates the sum of all integers from 1 up to a user-input number using a do-while loop.
 * Author      : Adarsh Venugopal (https://github.com/AVM-27)
 * Date        : 2025-08-02
 *
 * Program Details:
 *  - Prompts the user to input the last number n.
 *  - Uses a do-while loop to calculate the sum of integers from 1 to n.
 *  - Prints the resulting sum.
 *
 * Input:
 *  - A positive integer n.
 *
 * Output:
 *  - The sum of all integers from 1 to n.
 *
 * Sample Input:
 *  Input the last number
 *  10
 *
 * Sample Output:
 *  The sum of all numbers from 1 to 10 is 55
 */

#include <stdio.h>

int main()
{
    int i, n, sum;

    printf("Input the last number:\n");
    scanf("%d", &n);

    sum = 0;
    i = 1;

    do
    {
        sum += i;
        i++;
    } while (i <= n);

    printf("The sum of all numbers from 1 to %d is %d\n", n, sum);

    return 0;
}
